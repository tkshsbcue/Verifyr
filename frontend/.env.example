# Frontend (Vite) — copy to frontend/.env. These are PUBLIC client values
# (the publishable/anon key is designed to be exposed in the browser bundle).
VITE_SUPABASE_URL=https://ehigvycaojksnnrudlly.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_fftZeIM_EMzb1jFPDKgqFQ_e8k_bpnr

# Optional: point the SPA at a non-same-origin backend (default: same origin).
# VITE_API_BASE=http://127.0.0.1:8000
