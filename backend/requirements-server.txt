# Verifyr server (GUI backend) dependencies — install on top of requirements.txt
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
SQLAlchemy>=2.0.0
pydantic[email]>=2.6.0
httpx>=0.27.0                   # Supabase auth-token verification + Storage REST calls
APScheduler>=3.10.0
python-multipart>=0.0.9
psycopg[binary]>=3.1.0          # Postgres driver (for DATABASE_URL=postgresql+psycopg://...)
pyaxmlparser>=0.3.30            # parse APK package/version/label without the Android SDK
pytest>=8.0.0                   # test suite (cd backend && python -m pytest)
